import React,{useEffect,useState} from 'react';
import {Users,Plus,Search,Trash2} from 'lucide-react';
import {Card,PageTitle,Modal,Field,Badge,AskAgentButton} from '../components/UI';
import {apiRequest} from '../api';
const EMAIL_RE=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export default function Faculty(){const [items,setItems]=useState([]),[q,setQ]=useState(''),[open,setOpen]=useState(false),[form,setForm]=useState({id:'',name:'',email:'',expertise:''}),[error,setError]=useState('');const load=async()=>{try{const r=await apiRequest({method:'GET',url:'/data/faculty'});setItems(r.data||[])}catch(e){setError(e?.response?.data?.message||'Could not load faculty')}};useEffect(()=>{load()},[]);const add=async()=>{if(!form.id.trim())return setError('Faculty ID is required');if(!form.name.trim())return setError('Faculty name is required');if(!form.email.trim())return setError('Faculty email is required');if(!EMAIL_RE.test(form.email.trim()))return setError('Enter a valid faculty email address');try{const r=await apiRequest({method:'POST',url:'/data/faculty',data:{facultyId:form.id.trim(),name:form.name,email:form.email.trim(),expertise:form.expertise.split(',').map(x=>x.trim()).filter(Boolean),specializations:form.expertise.split(',').map(x=>x.trim()).filter(Boolean),qualifications:['M.Tech'],department:'CSE'}});setItems(v=>[...v,r.data]);setForm({id:'',name:'',email:'',expertise:''});setOpen(false);setError('')}catch(e){setError(e?.response?.data?.message||'Could not add faculty. Check the backend connection.')}};const remove=async(id)=>{if(!confirm('Deactivate this faculty member?'))return;try{await apiRequest({method:'DELETE',url:`/data/faculty/${id}`});setItems(v=>v.filter(x=>x.facultyId!==id))}catch(e){setError(e?.response?.data?.message||'Could not remove faculty')}};return <div><PageTitle eyebrow="FACULTY DIRECTORY" title="Faculty" desc="Manage faculty profiles, expertise and current teaching workload used by the allocation agent." action={<button type="button" onClick={()=>setOpen(true)} className="px-4 py-2.5 rounded-xl btn-primary text-sm flex items-center gap-2"><Plus size={17}/> Add faculty</button>}/>{error&&<div className="mb-4 p-3 rounded-xl alert-error text-sm">{error}</div>}<Card><div className="p-4 border-b border-slate-200"><div className="relative max-w-xl"><Search size={17} className="absolute left-3 top-3 text-slate-500"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search faculty or expertise..." className="field-input w-full pl-9 pr-3 py-2.5 rounded-xl outline-none"/></div></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4 p-5">{items.filter(x=>`${x.facultyId} ${x.name} ${(x.expertise||[]).join(' ')}`.toLowerCase().includes(q.toLowerCase())).map(x=><div key={x.facultyId} className="p-4 rounded-2xl border border-slate-200 bg-white/[0.02] card-hover group"><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-xl bg-cyan-400/10 text-cyan-700 flex items-center justify-center icon-pop"><Users size={20}/></div><div><div className="font-semibold text-slate-800">{x.name}</div><div className="text-xs text-slate-500">{x.facultyId}</div></div></div>{x.email&&<div className="mt-2 text-xs text-slate-500 truncate">{x.email}</div>}<div className="mt-4 text-xs text-slate-500">Expertise</div><div className="text-sm font-medium mt-1 text-slate-600">{(x.expertise||[]).join(', ')||'Not specified'}</div><div className="mt-3 flex justify-between items-center"><Badge tone="green">Workload {x.currentWorkload||0}/{x.maxWorkload||18}</Badge><button type="button" onClick={()=>remove(x.facultyId)} className="p-2 rounded-lg text-rose-700 hover:bg-rose-400/10" title="Deactivate"><Trash2 size={15}/></button></div><div className="mt-3 flex justify-end"><AskAgentButton prompt={`Analyze ${x.name} (${x.facultyId}) and tell me their current workload, expertise fit and best course matches.`} label="Ask agent"/></div></div>)}</div></Card><Modal open={open} title="Add faculty" onClose={()=>setOpen(false)}><div className="space-y-3"><Field label="Faculty ID" required value={form.id} onChange={e=>setForm({...form,id:e.target.value})} placeholder="F007"/><Field label="Faculty name" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Dr. New Faculty"/><Field label="Email" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="faculty@vignan.ac.in"/><Field label="Expertise" value={form.expertise} onChange={e=>setForm({...form,expertise:e.target.value})} placeholder="Machine Learning, AI"/></div><button type="button" onClick={add} className="mt-5 w-full py-2.5 rounded-xl btn-primary text-sm">Add faculty</button></Modal></div>}
