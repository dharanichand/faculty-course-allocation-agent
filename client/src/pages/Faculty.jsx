import React,{useEffect,useState} from 'react';
import {Users,Plus,Search,Trash2} from 'lucide-react';
import {Card,PageTitle,Modal,Field,Badge} from '../components/UI';
import {apiRequest} from '../api';
export default function Faculty(){const [items,setItems]=useState([]),[q,setQ]=useState(''),[open,setOpen]=useState(false),[form,setForm]=useState({id:'',name:'',expertise:''}),[error,setError]=useState('');const load=async()=>{try{const r=await apiRequest({method:'GET',url:'/data/faculty'});setItems(r.data||[])}catch(e){setError(e?.response?.data?.message||'Could not load faculty')}};useEffect(()=>{load()},[]);const add=async()=>{if(!form.name.trim())return setError('Faculty name is required');try{const r=await apiRequest({method:'POST',url:'/data/faculty',data:{facultyId:form.id||undefined,name:form.name,expertise:form.expertise.split(',').map(x=>x.trim()).filter(Boolean),specializations:form.expertise.split(',').map(x=>x.trim()).filter(Boolean),qualifications:['M.Tech'],department:'CSE'}});setItems(v=>[...v,r.data]);setForm({id:'',name:'',expertise:''});setOpen(false);setError('')}catch(e){setError(e?.response?.data?.message||'Could not add faculty. Check the backend connection.')}};const remove=async(id)=>{if(!confirm('Deactivate this faculty member?'))return;try{await apiRequest({method:'DELETE',url:`/data/faculty/${id}`});setItems(v=>v.filter(x=>x.facultyId!==id))}catch(e){setError(e?.response?.data?.message||'Could not remove faculty')}};return <div><PageTitle eyebrow="FACULTY DIRECTORY" title="Faculty" desc="Manage faculty profiles, expertise and current teaching workload used by the allocation agent." action={<button type="button" onClick={()=>setOpen(true)} className="px-4 py-2.5 rounded-xl bg-[#1d4ed8] text-white text-sm font-semibold flex items-center gap-2"><Plus size={17}/> Add faculty</button>}/>{error&&<div className="mb-4 p-3 rounded-xl bg-red-50 border border-red-200 text-red-700 text-sm">{error}</div>}<Card><div className="p-4 border-b"><div className="relative max-w-xl"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search faculty or expertise..." className="w-full pl-9 pr-3 py-2.5 rounded-xl border border-slate-200 outline-none"/></div></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4 p-5">{items.filter(x=>`${x.facultyId} ${x.name} ${(x.expertise||[]).join(' ')}`.toLowerCase().includes(q.toLowerCase())).map(x=><div key={x.facultyId} className="p-4 rounded-2xl border border-slate-200"><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center"><Users size={20}/></div><div><div className="font-bold">{x.name}</div><div className="text-xs text-slate-500">{x.facultyId}</div></div></div><div className="mt-4 text-xs text-slate-500">Expertise</div><div className="text-sm font-medium mt-1">{(x.expertise||[]).join(', ')||'Not specified'}</div><div className="mt-3 flex justify-between items-center"><Badge tone="green">Workload {x.currentWorkload||0}/{x.maxWorkload||18}</Badge><button type="button" onClick={()=>remove(x.facultyId)} className="p-2 rounded-lg text-red-500 hover:bg-red-50" title="Deactivate"><Trash2 size={15}/></button></div></div>)}</div></Card><Modal open={open} title="Add faculty" onClose={()=>setOpen(false)}><div className="space-y-3"><Field label="Faculty ID (optional)" value={form.id} onChange={e=>setForm({...form,id:e.target.value})} placeholder="F007"/><Field label="Faculty name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Dr. New Faculty"/><Field label="Expertise" value={form.expertise} onChange={e=>setForm({...form,expertise:e.target.value})} placeholder="Machine Learning, AI"/></div><button type="button" onClick={add} className="mt-5 w-full py-2.5 rounded-xl bg-[#1d4ed8] text-white font-semibold">Add faculty</button></Modal></div>}
